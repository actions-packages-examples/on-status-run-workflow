name: Main

on: status

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Dump GitHub Context
      env:
        GITHUB_CONTEXT: ${{ toJson(github) }}
      run: echo $GITHUB_CONTEXT
    - name: Show event payload only
      run: echo $GITHUB_EVENT_PATH | jq .
